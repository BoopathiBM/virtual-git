# ==============================
# Application Name
# ==============================
spring.application.name=virtualGit

# ==============================
# Server Port
# ==============================
server.port=8080

# ==============================
# MySQL Database Configuration
# ==============================
spring.datasource.url=jdbc:mysql://localhost:3306/virtual_git?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Bm@19026452
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==============================
# Hibernate / JPA
# ==============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ==============================
# Spring Batch
# ==============================
spring.batch.job.enabled=true
spring.batch.initialize-schema=never

# ==============================
# JWT Security (Optional Template)
# ==============================
jwt.secret=your_jwt_secret_key
jwt.expiration=3600000  # 1 hour in ms

# ==============================
# Logging (Optional)
# ==============================
logging.level.root=INFO
logging.level.com.virtualGit=DEBUG
